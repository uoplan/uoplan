.worktrees
node_modules
.cache
.pnpm-store
.vscode
dist
*.pdf
.DS_Store
.eslintcache
*.tsbuildinfo
.env*
.dev.vars*
!.dev.vars.example
playground
worker-configuration.d.ts
.wrangler
playwright-report
test-results
.vitest-attachments
__screenshots__
*.traineddata

# Test-coverage artifacts (Vitest v8 + Rust cargo-llvm-cov).
coverage/
lcov.info

# Runtime-coverage artifacts (vite-plugin-istanbul dead-code hunting).
.nyc_output/
coverage-runtime/

# Lighthouse CI reports (generated by `pnpm lighthouse`).
.lighthouseci/

# Raw grade-distribution data consumed by the grades scraper: the registrar
# Excel exports under raw/xlsx/ plus the per-term CSVs generated from them by
# `pnpm --filter scraper grades:convert`.
apps/scraper/data/raw/