name: Daily catalogue and schedule scrape

on:
  schedule:
    # 05:00 UTC ≈ midnight America/Toronto (standard) / 1am (DST)
    - cron: "0 5 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  daily-scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node 24
        uses: actions/setup-node@v4
        with:
          node-version: 24.x
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Backup existing JSON data
        run: |
          cp public/data/catalogue.json /tmp/catalogue_old.json
          cp public/data/terms.json /tmp/terms_old.json || true
          mkdir -p /tmp/schedules_old
          shopt -s nullglob
          for f in public/data/schedules.*.json; do
            cp "$f" "/tmp/schedules_old/$(basename "$f")"
          done
          shopt -u nullglob

      - name: Clear scraper caches (no cache mode)
        run: rm -rf .cache

      - name: Run catalogue scraper
        run: pnpm run scrape:catalogue

      - name: Run schedule scraper
        run: pnpm run scrape:schedules

      - name: Show JSON diffs (catalogue and schedules)
        run: |
          echo "::group::Diff catalogue.json"
          diff -u /tmp/catalogue_old.json public/data/catalogue.json || true
          echo "::endgroup::"
          echo "::group::Diff terms.json"
          diff -u /tmp/terms_old.json public/data/terms.json || true
          echo "::endgroup::"
          echo "::group::Diff schedules.*.json"
          shopt -s nullglob
          for f in public/data/schedules.*.json; do
            old="/tmp/schedules_old/$(basename "$f")"
            if [ -f "$old" ]; then
              diff -u "$old" "$f" || true
            else
              echo "New file: $f"
            fi
          done
          shopt -u nullglob
          echo "::endgroup::"

      - name: Check for data changes
        id: check_changes
        run: |
          if git diff --quiet -- public/data/catalogue.json public/data/terms.json public/data/schedules.*.json; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push updated data
        if: steps.check_changes.outputs.changed == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data/catalogue.json public/data/terms.json public/data/schedules.*.json
          git commit -m "chore: update catalogue and schedules (daily scrape)"
          git push origin HEAD:main

