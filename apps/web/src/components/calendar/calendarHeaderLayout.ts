/** Shared min-height (px) for the calendar header and the swap-panel header so
 * the two line up exactly when cross-fading between the calendar and swap views. */
export const CALENDAR_HEADER_MIN_HEIGHT = 53;
