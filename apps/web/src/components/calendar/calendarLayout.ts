/** Desktop sidebar width for calendar pages (Basic + Advanced). */
export const CALENDAR_SIDEBAR_WIDTH_PX = 380;

/** Default width (px) of the desktop week-preview bar (strip of mini week cards). */
export const CALENDAR_PREVIEW_BAR_WIDTH_PX = 120;
/** Wider default for large screens (see CALENDAR_PREVIEW_BAR_LARGE_QUERY). */
export const CALENDAR_PREVIEW_BAR_WIDTH_LARGE_PX = 160;
/** Media query that triggers the wider large-screen default. */
export const CALENDAR_PREVIEW_BAR_LARGE_QUERY = "(min-width: 1600px)";
/** Clamp bounds (px) for the resizeable week-preview bar. */
export const CALENDAR_PREVIEW_BAR_MIN_PX = 96;
export const CALENDAR_PREVIEW_BAR_MAX_PX = 280;
/** Landscape aspect ratio (height / width) for the mini week cards. */
export const CALENDAR_PREVIEW_CARD_ASPECT = 56 / 80;
